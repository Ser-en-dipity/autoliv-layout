import { ShapeLabelApiPipe } from './shape-label-api.pipe';

describe('ShapeLabelApiPipe', () => {
  it('create an instance', () => {
    const pipe = new ShapeLabelApiPipe();
    expect(pipe).toBeTruthy();
  });
});
