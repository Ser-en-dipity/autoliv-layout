import { FinishLabelPipe } from './finish-label.pipe';

describe('FinishLabelPipe', () => {
  it('create an instance', () => {
    const pipe = new FinishLabelPipe();
    expect(pipe).toBeTruthy();
  });
});
