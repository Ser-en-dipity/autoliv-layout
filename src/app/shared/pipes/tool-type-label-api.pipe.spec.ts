import { ToolTypeLabelApiPipe } from './tool-type-label-api.pipe';

describe('ToolTypeLabelApiPipe', () => {
  it('create an instance', () => {
    const pipe = new ToolTypeLabelApiPipe();
    expect(pipe).toBeTruthy();
  });
});
