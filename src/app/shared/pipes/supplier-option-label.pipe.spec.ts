import { SupplierOptionLabelPipe } from './supplier-option-label.pipe';

describe('SupplierOptionLabelPipe', () => {
  it('create an instance', () => {
    const pipe = new SupplierOptionLabelPipe();
    expect(pipe).toBeTruthy();
  });
});
