import { UnitLabelGqlPipe } from './unit-label-gql.pipe';

describe('UnitLabelGqlPipe', () => {
  it('create an instance', () => {
    const pipe = new UnitLabelGqlPipe();
    expect(pipe).toBeTruthy();
  });
});
