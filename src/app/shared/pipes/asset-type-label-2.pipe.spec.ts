import { AssetTypeLabel2Pipe } from './asset-type-label-2.pipe';

describe('AssetTypeLabel2Pipe', () => {
  it('create an instance', () => {
    const pipe = new AssetTypeLabel2Pipe();
    expect(pipe).toBeTruthy();
  });
});
