import { StateLabel2Pipe } from './state-label-2.pipe';

describe('StateLabel2Pipe', () => {
  it('create an instance', () => {
    const pipe = new StateLabel2Pipe();
    expect(pipe).toBeTruthy();
  });
});
