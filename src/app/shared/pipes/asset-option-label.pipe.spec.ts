import { AssetOptionLabelPipe } from './asset-option-label.pipe';

describe('AssetOptionLabelPipe', () => {
  it('create an instance', () => {
    const pipe = new AssetOptionLabelPipe();
    expect(pipe).toBeTruthy();
  });
});
