import { SearchOptionLabelPipe } from './search-option-label.pipe';

describe('SearchOptionLabelPipe', () => {
  it('create an instance', () => {
    const pipe = new SearchOptionLabelPipe();
    expect(pipe).toBeTruthy();
  });
});
