import { MachineOptionLabelPipe } from './machine-option-label.pipe';

describe('MachineOptionLabelPipe', () => {
  it('create an instance', () => {
    const pipe = new MachineOptionLabelPipe();
    expect(pipe).toBeTruthy();
  });
});
