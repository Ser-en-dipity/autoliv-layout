import { PoStateLabelPipe } from './po-state-label.pipe';

describe('PoStateLabelPipe', () => {
  it('create an instance', () => {
    const pipe = new PoStateLabelPipe();
    expect(pipe).toBeTruthy();
  });
});
