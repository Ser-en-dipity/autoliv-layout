import { SemiProductLabelPipe } from './semi-product-label.pipe';

describe('SemiProductLabelPipe', () => {
  it('create an instance', () => {
    const pipe = new SemiProductLabelPipe();
    expect(pipe).toBeTruthy();
  });
});
