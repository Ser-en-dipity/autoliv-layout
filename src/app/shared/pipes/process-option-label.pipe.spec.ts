import { ProcessOptionLabelPipe } from './process-option-label.pipe';

describe('ProcessOptionLabelPipe', () => {
  it('create an instance', () => {
    const pipe = new ProcessOptionLabelPipe();
    expect(pipe).toBeTruthy();
  });
});
