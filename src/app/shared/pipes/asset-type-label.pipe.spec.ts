import { AssetTypeLabelPipe } from './asset-type-label.pipe';

describe('AssetTypeLabelPipe', () => {
  it('create an instance', () => {
    const pipe = new AssetTypeLabelPipe();
    expect(pipe).toBeTruthy();
  });
});
