import { ProductOptionLabelPipe } from './product-option-label.pipe';

describe('ProductOptionLabelPipe', () => {
  it('create an instance', () => {
    const pipe = new ProductOptionLabelPipe();
    expect(pipe).toBeTruthy();
  });
});
