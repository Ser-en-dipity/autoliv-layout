import { TaxLabelGqlPipe } from './tax-label-gql.pipe';

describe('TaxLabelGqlPipe', () => {
  it('create an instance', () => {
    const pipe = new TaxLabelGqlPipe();
    expect(pipe).toBeTruthy();
  });
});
