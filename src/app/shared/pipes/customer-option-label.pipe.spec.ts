import { CustomerOptionLabelPipe } from './customer-option-label.pipe';

describe('CustomerOptionLabelPipe', () => {
  it('create an instance', () => {
    const pipe = new CustomerOptionLabelPipe();
    expect(pipe).toBeTruthy();
  });
});
