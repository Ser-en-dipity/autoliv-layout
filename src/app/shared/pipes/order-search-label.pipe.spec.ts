import { OrderSearchLabelPipe } from './order-search-label.pipe';

describe('OrderSearchLabelPipe', () => {
  it('create an instance', () => {
    const pipe = new OrderSearchLabelPipe();
    expect(pipe).toBeTruthy();
  });
});
