import { RawLabelApiPipe } from './raw-label-api.pipe';

describe('RawLabelApiPipe', () => {
  it('create an instance', () => {
    const pipe = new RawLabelApiPipe();
    expect(pipe).toBeTruthy();
  });
});
