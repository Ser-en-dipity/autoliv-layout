import { StockChangeTypePipe } from './stock-change-type.pipe';

describe('StockChangeTypePipe', () => {
  it('create an instance', () => {
    const pipe = new StockChangeTypePipe();
    expect(pipe).toBeTruthy();
  });
});
