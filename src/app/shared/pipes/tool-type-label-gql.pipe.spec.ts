import { ToolTypeLabelGqlPipe } from './tool-type-label-gql.pipe';

describe('ToolTypeLabelGqlPipe', () => {
  it('create an instance', () => {
    const pipe = new ToolTypeLabelGqlPipe();
    expect(pipe).toBeTruthy();
  });
});
