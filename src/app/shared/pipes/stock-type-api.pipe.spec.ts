import { StockTypeApiPipe } from './stock-type-api.pipe';

describe('StockTypeApiPipe', () => {
  it('create an instance', () => {
    const pipe = new StockTypeApiPipe();
    expect(pipe).toBeTruthy();
  });
});
