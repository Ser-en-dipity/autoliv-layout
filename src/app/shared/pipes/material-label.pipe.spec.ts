import { MaterialLabelPipe } from './material-label.pipe';

describe('MaterialLabelPipe', () => {
  it('create an instance', () => {
    const pipe = new MaterialLabelPipe();
    expect(pipe).toBeTruthy();
  });
});
