import { StockTypeLabelPipe } from './stock-type-label.pipe';

describe('StockTypeLabelPipe', () => {
  it('create an instance', () => {
    const pipe = new StockTypeLabelPipe();
    expect(pipe).toBeTruthy();
  });
});
